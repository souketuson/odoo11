[options]
; This is the password that allows database operations:
; admin_passwd = admin
db_host = localhost
db_port = 5432
db_user = odoo
db_password = odoo
xmlrpc_port = 8069
pg_path = C:\Program Files\PostgreSQL\9.6\bin
addons_path = addons,odoo\addons,,..\addons_yc,..\addons

python odoo-bin shell -c odoo11.conf -d 資料庫名稱 --shell-interface ipython

odoo directory structure:	
odoo11 ┬─ \.git        -- git control file
       ├─ \addons      -- standard or 3rd party app
       ├─ \addons_yc   -- project
       ├─ \odoo -11.0  -- odoo origin setting don't edit
       ├─ .gitignore   -- git ignore file or directory
       └─ description -- configuration

sorce code edit records:
1. odoo11(root directory)>odoo-11.0>addons>web>static>lib>bootstrap>css>bootstrap.css L#2285                  -- list 下框線改成黑色(目前沒作用)
2. odoo11(root directory)>odoo-11.0>odoo>http.py L#1095                                                                                         -- 動態過濾廠別
3. odoo11(root directory)>odoo-11.0>addons>web>static>src>js>views>list>list_editable_renderer.js L#518   -- xmany 新增項目前面添加 + 號
4. odoo11(root directory)>odoo-11.0>addons>web>static>src>js>fields>relational_fields.js L#113            -- o2m 增加下拉值
